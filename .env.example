# Copy to .env.local and fill in your Neon (or any PostgreSQL) connection string.
# Get it from: Neon dashboard → your project → Connection string
# Or: Vercel Dashboard → Storage → your Postgres DB → .env.local tab (copy DATABASE_URL)

DATABASE_URL="postgres://user:password@host/dbname?sslmode=require"
