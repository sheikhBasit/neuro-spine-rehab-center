import type { Metadata } from 'next'
export const metadata: Metadata = { title: 'Doctor Portal — Neuro Spine Rehab Center' }
export default function Layout({ children }: { children: React.ReactNode }) { return <>{children}</> }
