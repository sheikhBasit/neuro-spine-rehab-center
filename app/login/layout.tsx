import type { Metadata } from 'next'
export const metadata: Metadata = { title: 'Sign In — Neuro Spine Rehab Center' }
export default function Layout({ children }: { children: React.ReactNode }) { return <>{children}</> }
